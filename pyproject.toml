[project]
name = "rxdjango-dev"
version = "0.0.0"
description = "Development environment for rxdjango"
requires-python = ">=3.12"
dependencies = [
    "rxdjango",
]

[tool.uv.sources]
rxdjango = { path = "packages/python", editable = true }
