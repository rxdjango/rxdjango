# Goal

Implement the base ContextChannel class in packages/react and matching python functionality

# Requirements

- Implement the protocol defined in ADR 0002
  - Only implement enough to work with counter app: action call, value set
- The tests in backend/counter must pass after the implementation
